//! 库入口：声明项目的顶层模块树，负责导出可复用的业务模块
//!
//! `main.rs` 只负责启动 HTTP 服务，需要的业务代码都需要通过 `lib.rs` 导出
//! 使集成测试或其他二进制程序能够复用同一套应用逻辑

/// 导出配置模块，负责加载环境变量等启动配置
pub mod config;
/// 导出核心模块，存放全局错误、响应与共享状态
pub mod core;
/// 导出业务领域模块，按具体业务组织 HTTP 与数据访问逻辑
pub mod domains;
/// 导出基础设施模块，负责数据库等外部资源
pub mod infrastructure;
