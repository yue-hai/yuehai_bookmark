//! 本地环境变量文件的加载
//!
//! `.env` 仅用于本地开发；生产环境通常由容器、CI 或操作系统直接注入变量
//! 因此找不到该文件不是错误

/// 尝试加载项目根目录的 `.env` 文件
pub fn init() {
    // 调用 dotenvy 读取 `.env` 并写入当前进程环境变量
    // 使用 ok() 丢弃文件不存在等错误，以兼容生产环境直接注入配置的方式
    dotenvy::dotenv().ok();
}
